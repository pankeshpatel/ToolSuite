package fr.inria.arles.pankesh.sim;

public class OneTwo {

	public static void main(String[] args) {

		/* new sim.deviceD1.Startup().startDevice();
		 new sim.deviceD2.Startup().startDevice(); 
		 new sim.deviceD3.Startup().startDevice(); 
		 new sim.deviceD4.Startup().startDevice(); 
		 new sim.deviceD5.Startup().startDevice(); 
		 new sim.deviceD6.Startup().startDevice(); 
		 new sim.deviceD7.Startup().startDevice(); 
		 new sim.deviceD8.Startup().startDevice();*/
	}
}
